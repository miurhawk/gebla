import * as React from "react";
import { FlexColumnSpaceBetween } from "../styles";

const Login = () => {
  return <FlexColumnSpaceBetween>Log in section</FlexColumnSpaceBetween>;
};

export default Login;
